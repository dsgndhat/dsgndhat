language: ruby
sudo: false
before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version 1.7.0
  - export PATH=$HOME/.yarn/bin:$PATH
cache:
  yarn: true
script: 'yarn prod'

#after_success:
#  - chmod +x ./publish.sh
#  - ./publish.sh
